/// @file schema_registry.cpp
/// @brief DEP-003 SchemaRegistry implementation
///
/// This file exists primarily to ensure the header compiles correctly
/// and to provide a compilation unit for the schema module.

#include "dotvm/core/schema/schema_registry.hpp"

namespace dotvm::core::schema {

// SchemaRegistry is fully defined in the header as it's a template-heavy class.
// This file ensures the header compiles correctly and provides a compilation unit.

}  // namespace dotvm::core::schema
